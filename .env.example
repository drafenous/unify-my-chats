# Comma-separated list of allowed origins for CORS on API routes (optional)
CORS_ALLOW_ORIGINS=http://localhost:3000

# Upstash Redis (REST)
UPSTASH_REDIS_REST_URL=...
UPSTASH_REDIS_REST_TOKEN=...

# Optional: YouTube Data API v3 key for read-only live chat
YOUTUBE_API_KEY=...

# Kick Dev (https://docs.kick.com/)
KICK_CLIENT_ID=...
KICK_CLIENT_SECRET=...

# Public URL of kick webhook, use ngrok if localhost (ex.: ngrok, Fly, Vercel function URL)
KICK_WEBHOOK_URL=http://localhost:3000/api/kick/webhook
